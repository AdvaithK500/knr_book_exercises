#include <stdio.h>

// Exercise 1-1
//int main() is not necessary, similarly return 0 is not
// the type of main nd the exit code 0 is implicit by compiler
main() {
    printf("Hello World!\n");
    //return 0;
}
